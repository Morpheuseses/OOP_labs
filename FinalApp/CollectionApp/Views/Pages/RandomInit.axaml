<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:CollectionApp.ViewModels.Pages"
             x:Class="CollectionApp.Views.Pages.RandomInit"
             x:DataType="vm:RandomInitViewModel">
    <Grid RowDefinitions="Auto,Auto,Auto" ColumnDefinitions="300,*" Margin="20">
        <TextBlock Grid.Row="0" Text="Инициализация случайными элементами" Foreground="{DynamicResource PrimaryForeground}" FontSize="25"/>
        <TextBlock Text="Количество элементов:" 
                   Grid.Row="1" Grid.Column="0" 
                   VerticalAlignment="Center"
                   Foreground="{DynamicResource PrimaryForeground}"/>
        <TextBox Text="{Binding Count, Mode=TwoWay}" 
                 Grid.Row="1" Grid.Column="1" Width="200" HorizontalAlignment="Left"/>

        <Button Content="Сгенерировать"
                Command="{Binding RandomInitCommand}" 
                Grid.Row="2" Grid.Column="1"
                HorizontalAlignment="Left" 
                Margin="0,10,0,0" 
                Width="200"
                Foreground="{DynamicResource PrimaryForeground}"/>
    </Grid>
</UserControl>
